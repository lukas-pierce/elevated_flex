name: elevated_flex_examples
description: "Examples for the elevated_flex package."
homepage: https://github.com/lukas-pierce/elevated_flex

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.3.4 <4.0.0'

dependencies:
  elevated_flex: any
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  bloc: ^8.1.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

dependency_overrides:
  elevated_flex:
    path: ../../
