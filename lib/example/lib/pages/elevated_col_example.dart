import 'package:flutter/material.dart';

class ElevatedColExample extends StatelessWidget {
  const ElevatedColExample({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('ElevatedColExample'),
      ),
    );
  }
}
